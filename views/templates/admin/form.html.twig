{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
  {{ form_start(oAuthSignInConfigurationForm) }}
  <div class="card">
    <h3 class="card-header">
      <i class="material-icons">settings</i> {{ 'Google API configuration'|trans({}, 'Modules.FirstModule.Admin') }}
    </h3>
    <div class="card-body">
      <div class="form-wrapper">
        {{ form_widget(oAuthSignInConfigurationForm) }}
      </div>
    </div>
    <div class="card-footer">
      <div class="d-flex justify-content-end">
        <button class="btn btn-primary float-right" id="save-button">
          {{ 'Save'|trans({}, 'Admin.Actions') }}
        </button>
      </div>
    </div>
  </div>
  {{ form_end(oAuthSignInConfigurationForm) }}
{% endblock %}
